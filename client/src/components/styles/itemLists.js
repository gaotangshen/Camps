const styles = theme => ({
  root: {
    width: '1200px',
    marginTop: theme.spacing.unit * 3,
    overflowX: 'auto',
    margin: '30px auto',
  },
  table: {
    minWidth: 700,
  },
});

export default styles;
